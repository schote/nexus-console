# Configuration Loader

The `load_configuration` module provides a thin wrapper around PyYAML and Pydantic for loading and validating the hardware device configuration file (`device_config.yaml`) into a `NexusConfiguration` model.

---

::: console.utilities.load_configuration
