# Spin-Echo Projection

`se_projection` constructs a spin-echo sequence with a readout gradient for acquiring 1D projection profiles. The readout gradient is applied during the ADC gate, frequency-encoding spatial position along one axis.

---

::: console.utilities.sequences.spectrometry.se_projection
