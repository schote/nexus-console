# FID Transmit Calibration

`fid_tx_adjust` constructs a calibration sequence for determining the transmit (B1) gain. Multiple FID acquisitions are acquired at varying flip angles to characterise the relationship between `b1_scaling` and the effective flip angle.

---

::: console.utilities.sequences.calibration.fid_tx_adjust
